# GPU-enabled PyTorch lock for the NeuroMultiverse analysis environment.
#
# This file is separate from requirements-lock.txt because the PyTorch wheel
# index depends on the local NVIDIA driver and is not resolvable from PyPI.
# The versions below were read from the environment after a real installation.
# They are measurements, not estimates.
#
# Resolved and verified on:   2026-07-17
# Resolver command:
#     python -m pip install torch torchvision \
#         --index-url https://download.pytorch.org/whl/cu130
# Authoritative index:        https://download.pytorch.org/whl/cu130
# Python:                     3.11.9 (CPython, Windows x86_64)
# pip:                        26.1.2
# Operating system:           Windows 11 Home, build 26200, 64-bit
# NVIDIA driver (nvidia-smi): 610.47
# CUDA UMD reported by driver: 13.3
# torch CUDA build:           13.0
# Verified GPU:               NVIDIA GeForce RTX 4070 Laptop GPU
# torch.cuda.is_available():  True
#
# Reinstall with the index URL above; a bare `pip install -r` against PyPI
# resolves the CPU-only build instead.

torch==2.13.0+cu130
torchvision==0.28.0+cu130

# Direct dependencies pulled in by the PyTorch install, pinned as installed.
filelock==3.30.2
fsspec==2026.6.0
Jinja2==3.1.6
mpmath==1.3.0
networkx==3.6.1
pillow==12.2.0
sympy==1.14.0
typing_extensions==4.16.0
