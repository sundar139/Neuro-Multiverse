# Security and Data-Protection Practices

Version: 1.0
Document date: 2026-07-17
Governing protocol: [preregistration.md](preregistration.md)

This project handles secondary human neuroimaging data under data-use agreements. The controls below are obligations, not suggestions.

---

## 1. Secret handling

- No credential, token, API key, password, or private key is ever committed to this repository.
- No secret is pasted into a chat, an issue, a commit message, a log, or a report.
- Secrets are supplied through the local environment or an out-of-tree file, never through tracked configuration.
- No `.env` file exists in this repository, and none is needed for the current work. Repository ignore rules exclude `.env` and `.env.*` as defence in depth.
- Tracked files are scanned for credential patterns by `pre-commit` (`detect-private-key`) and by both verification scripts. These scans catch common shapes; they are a safety net, not a substitute for not writing the secret in the first place.

---

## 2. FreeSurfer license handling

fMRIPrep requires a FreeSurfer license file. It is licensed material and is treated as a secret.

- Location: `$HOME/licenses/license.txt` inside WSL2 — outside the repository entirely.
- Permissions: `600`, owner-only. Verified present with mode 600 on 2026-07-17.
- The repository ignore rules exclude `license.txt`. This is defence in depth, not permission to place the file in the working tree.
- The license contents are never printed, echoed, read aloud, copied into a report, pasted into a chat, hashed into a public document, or committed.
- Existence and permissions are verified without reading the contents. Check the mode only; do not print owner or registration fields:

```bash
stat -c "%a" "$HOME/licenses/license.txt"
```

---

## 3. Participant data protection

- No participant data appear in logs, error messages, exception text, commit messages, or reports.
- No attempt to re-identify any participant is permitted under any circumstance.
- Raw imaging is identifiable-adjacent and never enters version control.
- Only aggregate, disclosure-safe outputs may appear in any public report or artifact. No participant-level imaging, identifier, or reconstructable feature vector is published.
- Diagnostic labels are research labels. They are not clinical diagnoses, and this system is not for clinical use.
- Data-use agreements and licenses govern all sharing. Redistribution occurs only when the governing license explicitly permits it and the permission has been verified and recorded.

---

## 4. Large-file safeguards

Imaging data are large, and a single accidental commit is effectively permanent because Git history is immutable by policy here.

Layered defences:

1. `.gitignore` excludes data directories, imaging formats, array formats, model weights, caches, and archives.
2. `.gitattributes` marks those formats binary, so no text conversion masks them.
3. `pre-commit`'s `check-added-large-files` blocks anything over 1 MB from being committed.
4. Both verification scripts fail on any file over 5 MB outside ignored directories.
5. Both verification scripts fail on any tracked path matching an imaging, derivative, or model-weight pattern.

If a large file or dataset is committed by accident, stop and report it before pushing. Do not attempt a history rewrite unilaterally — this repository's history carries the evidentiary claim that the protocol was locked before modeling, and rewriting it damages that claim.

---

## 5. Dependency supply chain

- Python packages are installed from PyPI, and GPU-enabled PyTorch from the official PyTorch index at `https://download.pytorch.org/whl/`. No other index is configured.
- Dependencies are pinned in lock files generated by a real resolution, so an install is reproducible rather than whatever happens to be current.
- `pre-commit` hook revisions are pinned. They are updated deliberately, never automatically.
- Container images are pinned by immutable digest rather than by tag, because a tag can be repointed at different content.
- No downloaded script is executed without review. Piping a remote script directly into a shell is prohibited: it executes whatever the server returns at that moment, which is neither reviewed nor reproducible.
- Installation instructions are read from the official source of the tool at the time of installation, never from a third-party blog and never copied from a stale document.

---

## 6. Dataset acquisition safety

The ds000030 pilot executor (`scripts/acquire_ds000030_pilot.py`) is a network-facing tool and is gated accordingly.

- **Exact approval binding.** Execution requires a separate external approval record (mode 600) whose `decision` is `approved` and whose plan, storage-record, and executor-bundle SHA-256 digests match the reviewed evidence at exactly the current `HEAD`; ancestor approval is insufficient. A command-line flag cannot establish approval. Execution also requires a clean tree and index, the runtime target at the recorded external root's `ds000030/raw` directory, and Ubuntu-24.04 WSL2 where every execution-critical file mode is verifiably 600.
- **No persisted URLs.** The external plan holds no download URL. At execution time the resolver walks the pinned OpenNeuro GraphQL file tree and fails closed unless each path segment and the final path, object id, and size resolve exactly once. Object requests and every redirect must remain HTTPS under the observed OpenNeuro CRN object path or the `openneuro.org` S3 bucket path; shared-host access to any other bucket is rejected. Signed URLs are never printed, logged, or persisted.
- **Integrity, not size.** A file is complete only when its size and a recomputed SHA-256 match the strict external checksum manifest; a same-size file without a valid checksum is never trusted. Downloads stream to `.partial`, re-derive a suitable provider checksum when one exists, verify size, `fsync`, and atomically promote. Manifest writes are canonical and atomic; failed recording quarantines the promoted file, and other failures remove partial files. Manifest, lock, and JSON-Lines events stay outside Git with mode 600 and no URL or credential.
- **Single execution.** An atomic external lock allows one executor process. Every execution event carries the common scope, snapshot, plan digest, approval id, approved commit, and executor-bundle digest; failures produce a sanitized `run_failed` event.
- **Dry-run is a gate.** The default `--dry-run` performs zero provider body requests, prints only aggregate identifier-free output, and exits nonzero on any precondition failure.
- **Approval preflight is body-free.** `--validate-execution` is the mandatory final approval preflight. It runs the same approval, exact-code, storage, runtime, permission, manifest, lock-availability, metadata-resolution, and origin gates as execution; resolves all 22 objects through metadata; and opens zero object bodies. It neither acquires the production lock nor creates events, partials, or manifest entries. Ordinary dry-run refuses supplied approval or storage evidence instead of validating it incompletely.
- **Metadata redirects and file creation are closed.** GraphQL redirects must remain at the exact HTTPS metadata endpoint. Event logs and manifest temporary files are created at mode 600, and manifest replacement is followed by a parent-directory `fsync` where supported.
- **Bounded acquisition completed.** Approval `nm-ds000030-pilot-20260718-chatgpt-audit-001` served one successful 22-file, 187,570,603-byte transfer. All 22 external manifest hashes were independently recomputed; partial, quarantine, and integrity-failure counts were zero. No downloaded content was inspected. The evidence commit invalidates the old exact-HEAD approval for future use; ABIDE, COBRE, and the approximately 20-subject expansion remain unauthorized.
- **Raw validation fails closed.** Before raw inspection, the validator binds the actual manifest SHA-256 to the acquisition receipt and audited digest. It requires mode-700 directories and mode-600 evidence/raw files, verifies hashes before and after inspection, uses header-only NIfTI access, and has no repair mode. Production pins both the BIDS image `bids/validator@sha256:8ef7bf22a5e62430c98c0f3e62627f400c62e85c20db3f691e370ddfdc9963c7` and the reviewed Docker Desktop WSL CLI. Docker subprocesses reject inherited Docker overrides, receive only a fixed locale environment, use an empty system configuration location, and explicitly select the reviewed local Unix socket. The default context, Docker Desktop server identity, Linux/amd64 architecture, versions, API, and non-rootless mode must agree. CLI and endpoint identities are revalidated after every command before nonzero exits are enforced, so failed commands cannot bypass mutation checks. Docker Desktop, CLI, context, socket, or backend changes invalidate trust evidence. Container runs retain `--pull=never`, no network, and least privilege; the version probe receives no raw mount, while validation receives exactly one read-only raw mount. Permission correction is complete (22 files mode 600, 16 dirs mode 700; evidence `ds000030-pilot-permissions-sha256:aeb62b14a73926783543311e3953a1542f2dde5f57cb1b9a7e0216407157680e`). The raw validation completed at `2026-07-19T05:06:30.303343Z` with 22 files and 187,570,603 bytes validated, **0 errors, 139 warnings** (3 JSON_KEY_RECOMMENDED, 135 SIDECAR_KEY_RECOMMENDED, 1 README_FILE_MISSING), and **0 ignored** files. All 139 warnings are recommendations only and do not affect structural soundness. The BIDS Validator image (validator 3.0.0, BIDS schema 1.2.4) ran under `--network=none` and `--read-only`; no raw metadata was rewritten, no data was reacquired, and no voxel array was loaded. The canonical public evidence is the aggregate execution receipt `ds000030-pilot-raw-validation-sha256:b10cb77f6d2b8a5b3f9ca4154935b2d87eb2725d420f6e639d5bd9c0a9a51261`; the supporting artifacts stay separately pinned — Detailed report SHA-256: `41c31c09f0fbacc0425f2847333d5e20fd70edec4811a2a251cf5c134c5822ec`; Raw validator output SHA-256: `a7de9e4f9e345b3d78f24fccdf8cea5e2f30fe986914d8261b4624668b3f01e2`; Pre-validation snapshot SHA-256: `3ff13abfcc62b385594b09cbfcefefd187701a72c448b38506d97d12c954cd6d`. Preprocessing has not begun; ABIDE and COBRE remain blocked and the approximately 20-subject expansion remains unauthorized.

---

## 7. System boundary

This repository does not silently install or reconfigure system-wide software. WSL2 distributions, Docker Desktop, GPU drivers, R, FSL, AFNI, FreeSurfer, `.wslconfig`, global Git configuration, and global VS Code configuration are all outside its scope. Their status is discovered and reported; changing them is a deliberate manual action by the user.

---

## 8. Reporting an accidental exposure

If a secret, license, credential, or participant datum is exposed — committed, pushed, logged, or pasted — treat it as compromised. Assume disclosure; do not assume nobody noticed.

1. **Stop.** Do not push. Do not continue the task that caused it.
2. **Report it immediately** to the study lead, before attempting any remediation. A quiet cleanup destroys the evidence needed to judge the exposure's scope.
3. **Revoke and rotate** the exposed credential at its source. Rotation is what ends the exposure; removing the file does not, because the value is already out.
4. **Assess scope.** Determine whether the material reached a remote, a public repository, a log aggregator, a build service, or a third-party chat. A push to a public remote must be assumed to have been indexed.
5. **Record the incident**, including what was exposed, where it reached, when, and what was rotated. If a data-use agreement covers the exposed material, follow its notification requirements.
6. **Only then** decide on repository remediation, with the study lead. History rewriting is a joint decision, never a reflex.

For an exposed FreeSurfer license, notify the license issuer and request a replacement. For exposed participant data, the governing data-use agreement dictates the notification path, and the study lead executes it.
